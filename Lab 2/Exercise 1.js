// Rewrite the following code using ES6 features.
const numbers = [1, 2, 3, 4, 5];
for (const num of numbers) {
  console.log(`Number is ${num}`);
}
const square = n => n * n;
console.log(`Square of 4 is ${square(4)}`);